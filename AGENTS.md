# AGENTS.md

This project uses uv to manage it's python environment and dependencies.

## Testing instructions

To run a script in the repository, use the following command:
```bash
uv run <script.py>
```